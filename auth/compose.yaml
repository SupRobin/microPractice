services:
  mongodb:
    image: 'mongo:latest'
    environment:
      - 'MONGO_INITDB_DATABASE=mydatabase'
      - 'MONGO_INITDB_ROOT_PASSWORD=rootpass'
      - 'MONGO_INITDB_ROOT_USERNAME=root'
    ports:
      - '27017'
